name: CI

on:
  push:
    branches: [main, openwrt-24.10]
  pull_request:
    branches: [main, openwrt-24.10]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare:
    name: Prepare CI source
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v7

      - id: meta
        name: Resolve CI version
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          BASE_VERSION=$(sed -n 's/^PKG_VERSION:=//p' package/luci-theme-fluent/Makefile)
          TIMESTAMP=$(date -u +'%Y%m%d%H%M')
          if [ "$EVENT_NAME" = "pull_request" ]; then
            VERSION="${BASE_VERSION}_p${PR_NUMBER}"
          else
            VERSION="${BASE_VERSION}_p${TIMESTAMP}"
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Using CI version ${VERSION}"

      - name: Sync version files
        run: |
          VERSION="${{ steps.meta.outputs.version }}"
          sed -i "s/^PKG_VERSION:=.*/PKG_VERSION:=${VERSION}/" package/luci-theme-fluent/Makefile
          sed -i "s/^PKG_VERSION:=.*/PKG_VERSION:=${VERSION}/" package/luci-theme-fluent-lite/Makefile
          sed -i "s/^PKG_VERSION:=.*/PKG_VERSION:=${VERSION}/" package/luci-mod-fluentdashboard/Makefile
          sed -i "s/^  \"version\": \"[^\"]*\",/  \"version\": \"${VERSION}\",/" package.json
          grep '^PKG_VERSION:=' package/luci-theme-fluent/Makefile
          grep '^PKG_VERSION:=' package/luci-theme-fluent-lite/Makefile
          grep '^PKG_VERSION:=' package/luci-mod-fluentdashboard/Makefile
          grep '^  "version":' package.json

      - name: Pack prepared source
        run: tar --exclude=.git -czf "$RUNNER_TEMP/ci-source.tar.gz" .

      - name: Upload prepared source
        uses: actions/upload-artifact@v7
        with:
          name: ci-source-${{ github.run_id }}
          path: ${{ runner.temp }}/ci-source.tar.gz

  assets:
    name: Build minified assets
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - name: Download prepared source
        uses: actions/download-artifact@v8
        with:
          name: ci-source-${{ github.run_id }}

      - name: Extract prepared source
        run: tar -xzf ci-source.tar.gz

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build minified full and lite CSS & JS
        env:
          RSBUILD_MINIFY: "true"
        run: |
          pnpm run build
          pnpm run build:lite

      - name: Verify built assets and lite size reduction
        run: |
          test -s package/luci-theme-fluent/htdocs/luci-static/fluent/css/fluent.css
          test -s package/luci-theme-fluent/htdocs/luci-static/resources/menu-fluent.js
          test -s package/luci-theme-fluent/htdocs/luci-static/resources/view/fluent-config.js
          test -s package/luci-theme-fluent-lite/htdocs/luci-static/fluent/css/fluent.css
          test -s package/luci-theme-fluent-lite/htdocs/luci-static/resources/menu-fluent.js
          test -s package/luci-theme-fluent-lite/htdocs/luci-static/resources/view/fluent-config.js
          test -s package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard/index.js
          test -s package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard/index.css
          grep -q '"require network"' package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard/index.js
          grep -q 'return main;' package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard/index.js
          full_size=$(cat package/luci-theme-fluent/htdocs/luci-static/fluent/css/fluent.css package/luci-theme-fluent/htdocs/luci-static/resources/menu-fluent.js package/luci-theme-fluent/htdocs/luci-static/resources/view/fluent-config.js | wc -c)
          lite_size=$(cat package/luci-theme-fluent-lite/htdocs/luci-static/fluent/css/fluent.css package/luci-theme-fluent-lite/htdocs/luci-static/resources/menu-fluent.js package/luci-theme-fluent-lite/htdocs/luci-static/resources/view/fluent-config.js | wc -c)
          test "${lite_size}" -lt "${full_size}"
          ! grep -q 'fluent-menu-search\|mac-selector\|luci-theme-fluent update' package/luci-theme-fluent-lite/htdocs/luci-static/resources/menu-fluent.js
          echo "Full assets: ${full_size} bytes"
          echo "Lite assets: ${lite_size} bytes"

      - name: Upload full assets
        uses: actions/upload-artifact@v7
        with:
          name: fluent-assets-full
          path: |
            package/luci-theme-fluent/htdocs/luci-static/fluent/css/fluent.css
            package/luci-theme-fluent/htdocs/luci-static/resources/menu-fluent.js
            package/luci-theme-fluent/htdocs/luci-static/resources/view/fluent-config.js

      - name: Upload lite assets
        uses: actions/upload-artifact@v7
        with:
          name: fluent-assets-lite
          path: |
            package/luci-theme-fluent-lite/htdocs/luci-static/fluent/css/fluent.css
            package/luci-theme-fluent-lite/htdocs/luci-static/resources/menu-fluent.js
            package/luci-theme-fluent-lite/htdocs/luci-static/resources/view/fluent-config.js

      - name: Upload dashboard assets
        uses: actions/upload-artifact@v7
        with:
          name: fluent-dashboard-assets
          path: |
            package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard/index.js
            package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard/index.css

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint with Biome
        run: pnpm run lint

      - name: Type-check TypeScript
        run: pnpm run typecheck

      - name: Run focused tests
        run: pnpm run test

  openwrt-sdk:
    name: OpenWrt SDK Build (${{ matrix.version }}, ${{ matrix.variant }})
    needs: assets
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: ['24.10.7', '25.12.4']
        variant: [full, lite]
    steps:
      - name: Download prepared source
        uses: actions/download-artifact@v8
        with:
          name: ci-source-${{ github.run_id }}

      - name: Extract prepared source
        run: tar -xzf ci-source.tar.gz

      - name: Download full assets
        uses: actions/download-artifact@v8
        with:
          name: fluent-assets-full
          path: package/luci-theme-fluent/htdocs/luci-static

      - name: Download lite assets
        uses: actions/download-artifact@v8
        with:
          name: fluent-assets-lite
          path: package/luci-theme-fluent-lite/htdocs/luci-static

      - name: Download dashboard assets
        uses: actions/download-artifact@v8
        with:
          name: fluent-dashboard-assets
          path: package/luci-mod-fluentdashboard/htdocs/luci-static/resources/view/fluentdashboard

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y zstd wget

      - name: Prepare package sources
        run: |
          mkdir -p ${HOME}/builder/packages
          cp -r "$PWD/package/luci-theme-fluent" "${HOME}/builder/packages/luci-theme-fluent"
          cp -r "$PWD/package/luci-theme-fluent-lite" "${HOME}/builder/packages/luci-theme-fluent-lite"
          cp -r "$PWD/package/luci-mod-fluentdashboard" "${HOME}/builder/packages/luci-mod-fluentdashboard"
          echo "Staged packages:"
          ls -la ${HOME}/builder/packages/

      - name: Build packages
        run: |
          chmod +x .github/workflows/build.sh
          bash .github/workflows/build.sh "${{ matrix.version }}" "${{ matrix.variant }}"

      - name: Collect built packages
        run: |
          mkdir -p output
          cp ${HOME}/builder/output/luci-theme-fluent${{ matrix.variant == 'lite' && '-lite' || '' }}*.ipk output/ 2>/dev/null || true
          cp ${HOME}/builder/output/luci-theme-fluent${{ matrix.variant == 'lite' && '-lite' || '' }}*.apk output/ 2>/dev/null || true
          if [ "${{ matrix.variant }}" = "full" ]; then
            cp ${HOME}/builder/output/luci-mod-fluentdashboard*.ipk output/ 2>/dev/null || true
            cp ${HOME}/builder/output/luci-mod-fluentdashboard*.apk output/ 2>/dev/null || true
            cp ${HOME}/builder/output/luci-i18n-fluent*.ipk output/ 2>/dev/null || true
            cp ${HOME}/builder/output/luci-i18n-fluent*.apk output/ 2>/dev/null || true
          fi
          echo "Built packages:"
          ls -lh output/

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: packages-openwrt-${{ matrix.version }}-${{ matrix.variant }}
          path: output/*
          if-no-files-found: warn

  nightly:
    name: Nightly Release
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/openwrt-24.10')
    needs: [prepare, openwrt-sdk]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout source
        uses: actions/checkout@v7

      - name: Download all packages
        uses: actions/download-artifact@v8
        with:
          path: packages/
          pattern: packages-openwrt-*
          merge-multiple: true

      - name: List packages
        run: |
          echo "Nightly packages:"
          find packages/ -type f \( -name "*.ipk" -o -name "*.apk" \) -exec ls -lh {} \;

      - name: Generate Release Notes
        run: |
          python src/script/generate_release_notes.py \
            --tag "nightly" \
            --repo "${{ github.repository }}" \
            --version "${{ needs.prepare.outputs.version }}" \
            --commit "${{ github.sha }}" \
            --dir "packages" \
            --output "release_notes.md" \
            --is-nightly

      - name: Delete existing nightly tag
        uses: dev-drprasad/delete-tag-and-release@v1.1
        with:
          delete_release: true
          tag_name: nightly
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Nightly Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: nightly
          name: "Nightly Build"
          overwrite_files: true
          body_path: release_notes.md
          files: |
            packages/**/*.ipk
            packages/**/*.apk
          prerelease: true
