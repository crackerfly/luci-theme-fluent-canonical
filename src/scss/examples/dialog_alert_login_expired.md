# 登录过期提示（在dialog中）

``` html
<div class="modal" role="dialog" aria-modal="true">
    <h4>会话已过期</h4>
    <div class="alert-message warning">由于身份验证会话已过期，需要重新登录。</div>
    <div class="right">
        <div class="btn primary">去登录...</div>
    </div>
</div>
```
